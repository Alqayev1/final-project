import React from 'react';

import '../app_component/Header.css'

const Header = () => (
    <div className='header'>
    <h1 className="heading">
        <span className="light">Weather</span> Forecast
    </h1>
    </div>
);

export default Header;
